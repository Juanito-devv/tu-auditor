{
  "name": "tu-auditor-api-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "googleapis": "^140.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
